{% extends 'base.html.twig' %}

{%  block body %}
    <div>
        {% for question in questions %}
            <p>{{ question.text }}</p>
            <!--1er [0] réponses relatives a la quesiton 1 -->
            <!--2eme [0] bouton de réponse a question 1 -->

            {% for answer in answers %}
                {% for ans in answer %}

                    {% if ans.question.id == question.id %}
                     {{ ans.answerText }} {% set nbAns = 0 %}
                        {% for statistic in statistics %}
                            {% for stat in statistic %}
                                {% if ans.id == stat.answer_id %}
                                    {% set nbAns =  stat.count  %}
                                {% endif %}
                            {% endfor %}
                        {% endfor %}
                        {{ nbAns }}
                        <br />
                    {% endif %}
                {% endfor %}
            {% endfor %}
        {% endfor %}
    </div>

{% endblock %}